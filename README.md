# dataformat_sqlite

A Moodle data format plugin that exports data as a SQLite database dump.

## Description

This plugin allows Moodle to export data in [SQLite](https://www.sqlite.org/) format (.sqlite files).
The export creates a SQLite database with a single table `export` containing the exported data.
